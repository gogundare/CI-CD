pipeline {
   agent {  
       docker { image 'node:7-alpine' }
   }
   stages {
      stage('Hello') {
         steps {
            echo 'Hello World'
         }
    stages {
      stage { ('Test') {
            steps {
                sh 'node --version'
    }
      
      }
   }
}
}
}
}

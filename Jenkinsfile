pipeline {
   agent {  image 'node:7-alpine' }
   }
   stages {
      stage('Hello') {
         steps {
            echo 'Hello World'
         }
    stage { ('Test') {
            steps {
                sh 'node --version'
    }
      }
   }
}
}
